[project]
name = "agent-ground-up-quantize"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "datasets>=3.2",
    "llmcompressor>=0.12,<0.13",
    "peft>=0.14",
    "pyyaml>=6",
    "torch>=2.10,<2.13",
]
